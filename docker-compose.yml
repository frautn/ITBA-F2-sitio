services:
  webserver:
    image: nginx:alpine
    container_name: jsx_dev_server
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro